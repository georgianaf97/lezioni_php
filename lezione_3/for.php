<?php

    for( $i=0; < $_GET['id']; $i++ ) {
        echo $i;
        echo '<br>';
    }   

    // inizializza il ciclo contante parte da zero , il secondo membro è la condizione, essa 
    // viene verificata ad ogni iterazione, il terzo membro è l'incremento del contatore.
    // ++ sta per incremento, -- sta per decremento
    // $i++ è equivalente a $i = $i + 1